apiVersion: v2
name: app
description: Generic Kodik service chart — Deployment + Service + Ingress + HPA + ExternalSecret. Инстанцируется per-service через values.
type: application
version: 0.1.0
appVersion: "1.0"
